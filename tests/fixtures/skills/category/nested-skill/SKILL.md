---
name: nested-skill
description: Skill in a subdirectory to test recursive discovery
---

# Nested Skill

This skill lives in `category/nested-skill/` to test recursive directory walking.

```xml
<skill>
  <goal>Verify that skills in subdirectories are discovered correctly.</goal>
</skill>
```
